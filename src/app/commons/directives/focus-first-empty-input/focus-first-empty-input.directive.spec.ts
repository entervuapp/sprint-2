import { FocusFirstEmptyInputDirective } from './focus-first-empty-input.directive';

describe('FocusFirstEmptyInputDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusFirstEmptyInputDirective();
    expect(directive).toBeTruthy();
  });
});
